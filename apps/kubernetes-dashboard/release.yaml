apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubernetes-dashboard
spec:
  interval: 5m
  chart:
    spec:
      chart: kubernetes-dashoard
      # version: "7.x"
      sourceRef:
        kind: HelmRepository
        name: kubernetes-dashboard
      valuesFiles:
        - values.yaml
      interval: 1m
